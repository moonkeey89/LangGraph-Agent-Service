# Legacy hand-built Agent

`hand_built_agent/` 保存项目早期为学习 Agent 原理而手写的 Graph、Router、Registry、Executor、Memory 和 Tool Schema。

这些文件：

- 仅用于回顾架构演进；
- 不进入当前 `ai_agent_learning` 包；
- 不参与当前 LangGraph ReAct 执行链；
- 不保证与当前 `AgentState` 或项目依赖兼容。

当前可运行实现位于 `src/ai_agent_learning/`。
